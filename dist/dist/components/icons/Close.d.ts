import * as React from "react";
declare type CloseProps = {
    paddingTop: number;
    height: number;
    width: number;
};
declare type CloseState = {};
export declare class Close extends React.PureComponent<CloseProps, CloseState> {
    constructor(props: CloseProps);
    render(): JSX.Element;
}
export {};
