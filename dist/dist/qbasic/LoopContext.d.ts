/** @constructor */
export declare class LoopContext {
    counter: number;
    forLabel: number;
    nextLabel: number;
    endLabel: number;
    constructor(counter: number, forLabel: number, nextLabel: number, endLabel: number);
}
